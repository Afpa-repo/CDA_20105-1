<form method="post" action="{{ path('f_r_personnes_delete', {'id': f_r_personne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ f_r_personne.id) }}">
    <button class="btn">Delete</button>
</form>
